# 🚀 دليل رفع ونشر منظومة إدارة الخدمة الكنسية أونلاين

تم تجهيز المشروع بالكامل ليكون **تطبيقاً موحداً (Unified Full-Stack App)**؛ حيث يقوم سيرفر Node.js بتقديم واجهة React والـ API معاً من نفس الرابط، مما يمنحك:
- **رابط واحد فقط** يفتح المنظومة كاملة.
- **عدم وجود مشاكل CORS نهائياً**.
- **توافق تام مع الكوكيز (Cookies)** على الهواتف والمتصفحات دون حظر للطرف الثالث.
- **استضافة مجانية 100%** مدى الحياة على منصات مثل Render أو Railway.

---

## 🌟 الطريقة الأولى: الرفع السحابي الدائم 24/7 (موصى بها جداً عبر Render.com)

هذه الطريقة توفر رابطاً دائماً مثل:
`https://church-service-management.onrender.com`
يعمل طوال اليوم حتى لو أغلقت جهاز الكمبيوتر الخاص بك.

### الخطوة 1: رفع المشروع إلى حسابك على GitHub

1. افتح مبوبة الأوامر (Terminal) في مجلد المشروع الرئيسي، ونفذ الأوامر التالية:

```bash
# 1. تهيئة مستودع Git
git init

# 2. إضافة الملفات (تم حماية الـ .env تلقائياً بواسطة .gitignore)
git add .

# 3. حفظ التعديلات
git commit -m "feat: complete church management system with Arabic RTL & Supabase"
```

2. توجه إلى موقع [GitHub.com](https://github.com) وأنشئ مستودعاً جديداً (New Repository) وسمّه مثلاً `church-service-management`.
3. اربط المستودع وارفعه (استبدل الرابط برابط مستودعك):

```bash
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/church-service-management.git
git push -u origin main
```

---

### الخطوة 2: النشر على Render.com مجاناً

1. افتح موقع [Render.com](https://render.com) وسجّل الدخول مجاناً بحساب GitHub.
2. في لوحة التحكم، اضغط على زر **New +** ثم اختر **Web Service**.
3. اختر مستودع الـ GitHub الذي قمت برفعه للتو (`church-service-management`).
4. اضبط الإعدادات التالية (أو سيكتشفها Render تلقائياً عبر ملف `render.yaml` المرفق):
   - **Name**: `church-service` (أو أي اسم تفضله)
   - **Region**: `Frankfurt (EU Central)` (أقرب لمنطقتك وقريب لسيرفر Supabase)
   - **Branch**: `main`
   - **Runtime**: `Node`
   - **Build Command**: `npm install && npm run build`
   - **Start Command**: `npm start`
   - **Instance Type**: `Free`

5. انزل إلى قسم **Environment Variables** وأضف المتغيرات التالية:
   | المتغير (Key) | القيمة (Value) |
   | :--- | :--- |
   | `NODE_ENV` | `production` |
   | `DATABASE_URL` | رابط Supabase مع البورت `6543` وباسوردك المشفر `%205-eB_'(C9CHg8z%23F9%7D` |
   | `DIRECT_URL` | رابط Supabase مع البورت `5432` وباسوردك المشفر `%205-eB_'(C9CHg8z%23F9%7D` |
   | `JWT_SECRET` | أي نص سري عشوائي طويل لحماية تسجيل الدخول |
   | `COOKIE_SECRET` | أي نص سري عشوائي طويل للكوكيز |

6. اضغط **Create Web Service**!
   - سيقوم Render بتثبيت الحزم، وبناء الواجهة الأمامية والـ Backend، وتشغيل المنظومة.
   - خلال دقائق ستحصل على رابط مباشر يبدأ بـ `https://` يمكنك إرساله للآباء الكهنة والخدام فوراً!

---

## ⚡ الطريقة الثانية: رابط فوري وسريع للتجربة الآن (في 30 ثانية)

إذا كنت ترغب في إرسال رابط حالاً لأي شخص لتجربة المنظومة من هاتفه المحمول **دون الحاجة لرفع الكود على GitHub الآن** (طالما جهازك مفتوح والبرنامج يعمل):

### الخيار (أ): عبر Cloudflare Tunnel (سريع ومستقر ومجاني بدون تسجيل)
نفذ هذا الأمر في الـ Terminal:
```bash
npx untun@latest tunnel --port 5000
```
أو باستخدام localtunnel:
```bash
npx localtunnel --port 5000
```
سيعطيك الرابط فوراً مثل:
`https://brave-church-service.loca.lt`
أي شخص يفتح هذا الرابط من هاتفه سيدخل مباشرة على المنظومة!

---

## 👥 حسابات الدخول التجريبية الجاهزة للتجربة

عند فتح الرابط، يمكنك الضغط مباشرة على أزرار **«تبديل تجريبي سريع»** أو الدخول بالبيانات التالية (كلمة المرور موحدة للجميع: `Church@2026!`):

- **أمين عام الخدمة**: `01000000001`
- **أمين قطاع إعدادي**: `01000000002`
- **أمين مرحلة أولى إعدادي**: `01000000003`
- **مساعد أمين مرحلة**: `01000000004`
- **خادم فصل (أسرة)**: `01000000005`
