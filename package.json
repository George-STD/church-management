{
  "name": "church-service-management-system",
  "version": "1.0.0",
  "description": "Enterprise Church Service (خدمة) Management System with Arabic RTL Mobile-First Design & Cumulative RBAC",
  "private": true,
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm run dev --workspace=server",
    "dev:client": "npm run dev --workspace=client",
    "build": "npm run prisma:generate && npm run build --workspace=@church/shared && npm run build --workspace=@church/client && npm run build --workspace=@church/server",
    "start": "npm run start --workspace=@church/server",
    "test": "npm run test --workspaces --if-present",
    "prisma:generate": "npm run prisma:generate --workspace=server",
    "prisma:push": "npm run prisma:push --workspace=server",
    "prisma:seed": "npm run prisma:seed --workspace=server"
  },
  "keywords": [
    "church",
    "service",
    "management",
    "coptic",
    "sunday-school",
    "arabic",
    "rtl",
    "supabase",
    "prisma"
  ],
  "author": "Elite Engineering Team",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
